# 指南（人类版本）

> 给 agent 的安装合同在 [guide.agent.md](guide.agent.md)

## 心理预期

**不用通读skill原文**：  skill 正文的受众是agent不是人类，故不用看skill原文，除非debug和二改。真需要了解建议定点让agent讲。 

**可按需升级**： 不必一次全装，也研究不过来。  

## Ask yourself

1. 同时进行的项目和feature多吗？
2. 一个聊天窗 / 多个不同家的模型并行？
3. agent 的报告能不能读得过来？能不能读得懂？是否需要读懂？

| 状态 | 层级 | 安装内容（累积） |
|---|---|---|
|看不懂agent输出而且看到就烦|**L0 沟通层**|decode|
| 一个项目一个聊天窗，人可以一直盯着 | **L1 记录层** | + track-project、handoff |
| 多 **feature** 并行推进 | **L2 秩序层** | + doc-setup |
| 多 **agent** 并行，看不过来agent的报告 | **L3 车队层** | + orchestration  |
| 经常跑无人值守的长程任务 | **L4 备用件** | + batch-hook |

定好层级后告诉 agent ：「读 guide.agent.md，按 Lx 进行安装」。

---

## L0 沟通层：把 agent 报告翻译成人话

### 面向问题

- agent 交上来的报告充满了黑话和看不懂的概念。
- 或者反过来：报告事无巨细看着就头疼。

### skill 功能

**decode**：把 agent 产出重写成人类可读密度。

### 使用方式

| 场景 | 使用例 |
|---|---|
| 报告读不动 | `/decode 这段` 或 `/decode <文件路径>` |

---

## L1 记录层：让 agent 记住项目进度

### 面向问题

- 换窗口会很不安怕丢东西，但不换窗口上下文太长额度消耗吓人。 
- 新开窗口说「继续xx」，agent找不到上次进度如无头苍蝇乱撞。 
- 长对话被自动压缩（compact）之后，agent 突发降智，之前内容的偏好、思路、原因一概记不起。
- agent 自己写了一堆 `NOTES.md`、`TODO.md`、`progress.md` 散在各处无人能理清。
- 内置 memory 项目内容是零碎偏好混在一起，难以找全。

### skill 功能

**track-project**：每个项目一张「项目卡」，固定格式、固定位置。卡的上半部分是**人类视图**——30 秒读懂现状，外加「等你决策」清单暂存上次没裁定好的待办事项；
其他部分是给 agent 看的接续信息——下一步动作，仓库的存储目录，当前活跃threads，之前留下的问题等等。
新窗口直接发项目卡文件位置和`track-project`就能顺利接续之前的工作进度。 

**handoff**：压缩上下文前运行，把默认压缩必丢的东西——说话方式、每个决策背后的原因、还活着的后台状态、你聊到一半的思路——单独存一份文件，供新窗口接续。

### 使用方式

| 场景 | 使用例 |
|---|---|
| 手头项目建卡 | `/track-project 为当前项目建立项目卡` |
| 每次开工 | `/track-project 接续 <项目名>` |
| 干完一段落 | `/track-project 更新项目卡` |
| 准备压缩前 | `写 /handoff，更新项目卡和其他文档，然后再 compact` |

### 预期效果

新窗口agent可快速准确接续项目进度；
自己忘了项目是什么，下一步做什么，打开项目卡的人类视图两分钟 get。

**升层信号**：同时有三四个正在推进的项目卡，repo 里的文档开始互相矛盾 → 升级 L2。

---

## L2 秩序层：让项目留下可交接的文档

### 面向问题

- 项目开发时间长，repo 里 spec、笔记、旧方案混在一起
- 「按之前定的方案做」，agent 找到了一个月之前的文档。
- 「我不要圆角毛玻璃」交代了四遍，agent仍然我行我素。

### skill 功能

**doc-setup**：给 repo 立一个按**生命周期**分区的 doc 结构：
- `doc/spec/` 里放现在还有效的设计文件；
- `principle.md`，专门记「你和默认ai审美不一样的地方」用于持久化肘击。
- 冻结历史带日期存档，供后续debug溯源。

### 使用方式

| 场景 | 对 agent 说 |
|---|---|
| 新 repo / 没结构的 repo | `/doc-setup 给这个 repo 立 doc 结构` |
| 已有一堆旧文档 | `/doc-setup 审计现有的 doc 目录` |
| 一批工作收尾 | `/doc-setup 归档这批文档` |

### 理想效果

agent 不再引用错文档；同一条偏好你不再重复交代；隔段时间回到 repo，现行契约在哪、历史在哪，一句话说得清。

**升层信号**：你开了第二个 agent 窗口并行干活；或者开始用第二家的模型，隐约觉得「它们擅长的不一样」；或者最直接的——agent 产出的代码开始超过你 review 的容量 → 去 L3。

---

## L3 车队层：多 agent 分工与产出消化

### 面向问题

- 并行 agent 的产出超过了你逐行 review 的容量，你开始「闭着眼合并」。
- 两个 agent 改了同一处，合并时才发现。
- 你在用不止一家模型，但分工全凭手感。

### skill 功能

**orchestration**：你 + 车头 + workers 的三层结构。
- 你负责方向、品味、危险决策；
- 车头（你手上最强的模型）负责拆任务、写 prompt、派活、裁决合并；
- workers 按席位干活（实现 / QA / 评审 / 样式）。

### 核心原则

- **agent间的错误多样性很重要** 同一家模型的盲区高度重合——写代码的和审代码的如果是同一个模型，审查就是自己查自己的作业。实现和评审至少分开实例，最好用不同家的模型。
- **规则跟着角色走，不跟着模型走。** 模型每代都换，角色和纪律不换。换模型只改座位表，不改流程。

`reference/worker-profiles.md` 里有一份作者环境的车队配置示例，**会过期**，可用于参考给自己的 agent 建档（能力边界在哪、什么活派给谁、什么情况下会出问题），模型配置相同的时候开箱即用。


### 人需要做的（还是逃不掉！）

- **决策还得自己做。** 架构方向、危险操作、产品取舍——车头会停下来问你。毕竟烧掉的api账单是你自己的。
- **验收要你亲手做。** 在真机或真界面上点一遍，不能只看测试通过就算完。ai的测试水平仍然可疑。


### 使用方式

装的时候 agent 会引导你填**座位表**：你有哪些可派单的 worker（哪个应用、什么模型、怎么调用）、谁坐哪个席位。只有一个 worker 也可以。

| 场景 | prompt |
|---|---|
| 要派活之前 | `/orchestration`（加载 playbook，之后正常布置任务） |
| 报告读不动 | `/decode 这段` 或 `/decode <文件路径>` |

### 用起来之后，应该看到

worker 的原始输出你基本不用看，压力集中到车头那边；人的精力花在方向和验收上。playbook 里的具体纪律由车头执行，人只在需要做决定的时候被叫到。

---

## L4 备用件：批次执行硬闸

### 面向问题

- 你让 agent 跑一个大批次任务，去睡觉或者干别的。回来发现它早就停了——停在一句「要继续吗？」上，或者写了个漂亮的阶段总结然后等你鼓掌。
- 任务本身没出错，agent 也没崩溃，就是自己决定「到这里差不多了」然后收手了。

### skill 功能

**batch-hook**：一个机械闸门，防止 agent 在批次任务中途自己停下来。原理是在 harness 的 Stop hook 里检查账本（你的任务清单）——还有没勾完的项目，就不许停；真的被阻塞了，必须在账本里写明 `BLOCKED:` 并说明原因，才允许退出。

安全设计：闸门坏了（配置错误、哨兵文件丢了）一律放行——宁可 agent 提前停，也不困住整个会话。

### 边界（它救不了的事）

只防「agent 自己决定停下来」，不防进程死亡。终端关了、机器睡了、网断了，闸门管不了。真要过夜无人值守，在外面再套一层定时重启（cron 之类）。

不是所有 agent 框架都支持 Stop hook——没有这个机制的（比如当前的 Codex CLI），这套纪律只能靠 prompt 约束，没有机械强制。

### 使用方式

> 读 guide.agent.md，加装 L4。

注册 Stop hook 是**全局设置变更**，agent 必须向你出示确切的改动内容并拿到你的明确同意——它不该悄悄改，你也不该让它悄悄改。

| 场景 | 对 agent 说 |
|---|---|
| 开批次前 | `/batch-hook 装闸` |
| 关账 / 被阻塞之后 | `/batch-hook 拆闸` |
| 会话停不下来 | `/batch-hook 排查`（先查哨兵文件是否忘拆） |

---

## 维护注记

本文各层的组件清单与 guide.agent.md 的「层级→组件映射表」为有意双写：改一处，同步另一处。
